% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stl_plot.R
\name{dts_stl_plot}
\alias{dts_stl_plot}
\title{STL decomposition and plot
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}}
\usage{
dts_stl_plot(
  data,
  var_name = NULL,
  date_var = "date",
  .freq = NULL,
  .select = NULL,
  .report = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame or a time series object.}

\item{var_name}{The name of the variable to decompose (required if \code{data} is a data frame).}

\item{date_var}{The name of the date/time variable in the data frame (default is "date").}

\item{.freq}{The frequency of the time series (required if \code{data} is a data frame).}

\item{.select}{The variables to select from the data frame (used only if \code{data} is a data frame).}

\item{.report}{Logical. Should a summary of the decomposition be printed? Default is TRUE.}

\item{...}{Additional arguments passed to \code{stats::stl()}.}
}
\value{
A plot of the decomposed components and, if \code{.report = TRUE}, a list of STL objects.
}
\description{
This function performs STL (Seasonal-Trend Decomposition of time series) decomposition
on a time series and generates a plot of the decomposed components (trend, seasonal, and residual).
}
\examples{
\dontrun{
  data(economics, package = "ggplot2")
  df <- economics

  # Decompose and plot the unemployment rate
  dts_stl_plot(df, var_name = "unemploy", date_var = "date", .freq = 12)

  # Example with a time series object
  data(AirPassengers)
  dts_stl_plot(AirPassengers, .freq = 12)
}
}
